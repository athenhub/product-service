spring:
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

rabbit:
  stock:
    exchange: stock.exchange
    registered:
      routing-key: stock.registered
      queue: stock.registered.queue